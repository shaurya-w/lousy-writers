import { buildCategoryPage } from '../../lib/buildCategoryPage';

export const metadata = {
  title: 'Side Quests',
  description: 'Travels, experiments, and other detours.',
};

export default buildCategoryPage(
  'side-quests',
  'Side Quests',
);
