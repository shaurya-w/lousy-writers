import { buildCategoryPage } from '../../lib/buildCategoryPage';

export const metadata = {
  title: 'Academics',
  description: 'Notes on research, reading, and the academic grind.',
};

export default buildCategoryPage(
  'academics',
  'Academics',
);
